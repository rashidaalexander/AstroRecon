# ADR-003: Cross-repo ecosystem links

## Status

Accepted

## Context

Feeds OrbitalGraph with normalized assets and references SignalNebula for spectrum context.

## Decision

Use a small, deterministic dataset bundled with the repository so the project runs immediately after clone.

## Consequences

The repository behaves like a credible research prototype without requiring outside services.
